apiVersion: apps/v1
kind: Deployment
metadata: { name: order-service }
spec:
  replicas: 3
  selector: { matchLabels: { app: order-service } }
  template:
    metadata: { labels: { app: order-service } }
    spec:
      containers:
        - name: order-service
          image: ghcr.io/example/order-service:latest
          ports: [{ containerPort: 8085 }]
          readinessProbe: { httpGet: { path: /actuator/health, port: 8085 } }
          livenessProbe: { httpGet: { path: /actuator/health, port: 8085 } }
---
apiVersion: v1
kind: Service
metadata: { name: order-service }
spec:
  selector: { app: order-service }
  ports: [{ port: 80, targetPort: 8085 }]
