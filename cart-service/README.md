# Cart Service

Responsibilities:
- Maintain active shopping cart per user
- Add and retrieve cart items
- Publish cart business metrics

## API
- `POST /carts/{userId}/items`
- `GET /carts/{userId}`
